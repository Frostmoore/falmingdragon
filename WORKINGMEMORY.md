# Working Memory — FlamingDragon
> Gestito automaticamente da WorkingMemoryService via tool `working_memory_append`.
> Limite: ~10.000 token (euristica: strlen/4). Le voci più vecchie vengono rimosse automaticamente.
> NON modificare a mano — usa il tool `working_memory_append`.
> Lettura: tool `working_memory_read` (opzionale: last_lines=N).

<!-- Le voci vengono aggiunte qui sotto in ordine cronologico -->
